# 🕐 أوقات الصلاة - Prayer Times

<div align="center">

![React](https://img.shields.io/badge/React-19.2.0-61DAFB?style=for-the-badge&logo=react&logoColor=white)
![TypeScript](https://img.shields.io/badge/TypeScript-5.9.3-3178C6?style=for-the-badge&logo=typescript&logoColor=white)
![Vite](https://img.shields.io/badge/Vite-7.2.5-646CFF?style=for-the-badge&logo=vite&logoColor=white)
![License](https://img.shields.io/badge/License-MIT-green?style=for-the-badge)

**تطبيق ويب لعرض أوقات الصلاة لمدن فلسطين المختلفة**

[English README](./README-EN.md)

</div>

---

## 📋 نظرة عامة على المشروع

### وصف المشروع

أوقات الصلاة هو تطبيق ويب تفاعلي مبني بتقنية React و TypeScript يهدف إلى توفير أوقات الصلاة اليومية للمستخدمين في المدن الفلسطينية المختلفة. يستفيد التطبيق من واجهة برمجة التطبيقات (API) الخاصة بـ Aladhan للحصول على أوقات دقيقة ومحدثة للصلاة بناءً على الموقع الجغرافي.

### المشكلة التي يحلها المشروع

يواجه المسلمون في فلسطين والمنطقة العربية صعوبات متعددة في معرفة أوقات الصلاة بدقة، بما في ذلك:

- **غياب مرجع موثوق**:找不到 موثوق وموثوق للحصول على أوقات الصلاة
- **التغيير المستمر**: تتغير أوقات الصلاة يومياً مما يتطلب مصدراً دائماً ومحدثاً
- **اختلاف الحسابات**: تستخدم المنظمات المختلفة طرقاً متعددة لحساب أوقات الصلاة
- **سهولة الوصول**: يحتاج المستخدمون إلى واجهة سهلة الاستخدام ومتجاوبة

يحل هذا المشروع هذه المشكلات من خلال توفير واجهة بسيطة ودقيقة تعرض أوقات الصلاة باستخدام حسابات علمية معتمدة دولياً.

---

## ✨ الميزات الرئيسية

### 🕌 عرض أوقات الصلاة

يعرض التطبيق الأوقات الخمسة الأساسية للصلاة اليومية:

| الصلوات              | الوصف                                          |
| -------------------- | ---------------------------------------------- |
| **الفجر (Fajr)**     | وقت صلاة الفجر الذي يبدأ عند ظهور الفجر الصادق |
| **الشروق (Sunrise)** | وقت شروق الشمس                                 |
| **الظهر (Dhuhr)**    | وقت صلاة الظهر بعد الزوال                      |
| **العصر (Asr)**      | وقت صلاة العصر                                 |
| **المغرب (Maghrib)** | وقت صلاة المغرب عند غروب الشمس                 |
| **العشاء (Isha)**    | وقت صلاة العشاء                                |

### 🏙️ دعم_multiple المدن

يدعم التطبيق مجموعة متنوعة من المدن الفلسطينية:

- **يافا (Jaffa)**: المدينة الساحلية التاريخية
- **نابلس (Nablus)**: المدينة في شمال West Bank
- **الخليل (Hebron)**: المدينة في جنوب West Bank
- **رام الله (Ramallah)**: المدينة administrative
- **القدس (Jerusalem)**: المدينة المقدسة

### 📅 عرض التاريخ

يعرض التطبيق التاريخ الهجري والميلادي بشكل تلقوي باستخدام `toLocaleDateString()` مع locale المناسب.

### 📱 تصميم متجاوب

يتميز التطبيق بتصميم متجاوب يعمل على جميع الأجهزة:

- **أجهزة سطح المكتب**: تخطيط كامل مع تفاصيل واضحة
- **أجهزة لوحية**: تخطيط محسن للشاشات المتوسطة
- **هواتف ذكية**: تخطيط مخصص للشاشات الصغيرة

### ⚡ تحديث فوري

عند تغيير المدينة، يتم تحديث أوقات الصلاة تلقائياً دون إعادة تحميل الصفحة.

### 🔄 تحميل ديناميكي

يعرض حالة تحميل أثناء جلب البيانات من API.

---

## 🏗️ هيكل المشروع

```
Prayer-times/
├── 📁 src/
│   ├── 📄 main.tsx                 # نقطة الدخول الرئيسية للتطبيق
│   ├── 📄 App.tsx                  # المكون الرئيسي للتطبيق
│   ├── 📄 App.css                  # تنسيقات المكون الرئيسي
│   ├── 📄 prayerTimes.tsx          # مكون عرض أوقات الصلاة
│   └── 📄 prayerTimes.css          # تنسيقات أوقات الصلاة
├── 📄 index.html                   # ملف HTML الرئيسي
├── 📄 package.json                 # تبعيات المشروع وسكريبتات npm
├── 📄 tsconfig.json                # إعدادات TypeScript
├── 📄 tsconfig.app.json            # إعدادات TypeScript للتطبيق
├── 📄 tsconfig.node.json           # إعدادات TypeScript لـ Node.js
├── 📄 vite.config.ts               # إعدادات Vite
├── 📄 eslint.config.js             # إعدادات ESLint
├── 📄 .gitignore                   # ملفات ومجلدات تجاهلها Git
└── 📄 README.md                    # ملف التوثيق الرئيسي
```

### شرح المجلدات والملفات الرئيسية

#### 📁 src/

هذا المجلد يحتوي على جميع ملفات الكود المصدري للتطبيق:

- **[`main.tsx`](src/main.tsx)**: نقطة الدخول الرئيسية التي تقوم بـ render للتطبيق في DOM
- **[`App.tsx`](src/App.tsx)**: المكون الرئيسي الذي يدير حالة المدينة ويعرض الواجهة
- **[`App.css`](src/App.css)**: تنسيقات CSS للمكون الرئيسي
- **[`prayerTimes.tsx`](src/prayerTimes.tsx)**: مكون وظيفي يستقبل اسم المدينة كـ prop ويعرض أوقات الصلاة
- **[`prayerTimes.css`](src/prayerTimes.css)**: تنسيقات CSS لعرض أوقات الصلاة

#### 📄 ملفات التكوين

- **[`vite.config.ts`](vite.config.ts)**: إعدادات Vite لتكوين بيئة التطوير والبناء
- **[`tsconfig.json`](tsconfig.json)**: إعدادات TypeScript الأساسية
- **[`eslint.config.js`](eslint.config.js)**: إعدادات ESLint لضمان جودة الكود

---

## 🚀 خطوات التثبيت والإعداد

### المتطلبات السابقة

قبل البدء في إعداد المشروع، تأكد من توفر المتطلبات التالية:

| المتطلب     | الإصدار المطلوب | الوصف                    |
| ----------- | --------------- | ------------------------ |
| **Node.js** | ≥ 18.0.0        | بيئة تشغيل JavaScript    |
| **npm**     | ≥ 9.0.0         | مدير حزم Node.js         |
| **Git**     | ≥ 2.0.0         | نظام التحكم في الإصدارات |

### التحقق من المتطلبات

```bash
# التحقق من إصدار Node.js
node --version

# التحقق من إصدار npm
npm --version

# التحقق من إصدار Git
git --version
```

### خطوات التثبيت

#### 1. استنساخ المشروع

```bash
git clone https://github.com/username/Prayer-times.git
cd Prayer-times
```

#### 2. تثبيت التبعيات

```bash
npm install
```

سيقوم npm بتثبيت جميع التبعيات المحددة في `package.json`:

- **React**: ^19.2.0
- **ReactDOM**: ^19.2.0
- **Vite**: npm:rolldown-vite@7.2.5
- **TypeScript**: ~5.9.3
- **ESLint**: ^9.39.1

#### 3. تشغيل بيئة التطوير

```bash
npm run dev
```

سيتم تشغيل خادم التطوير على `http://localhost:5173`

#### 4. بناء المشروع للإنتاج

```bash
npm run build
```

#### 5. معاينة البناء الإنتاجي

```bash
npm run preview
```

### المتغيرات البيئية

لا يتطلب المشروع متغيرات بيئية في الوقت الحالي، لكن يمكنك إنشاء ملف `.env` إذا أردت تخصيص إعدادات API مستقبلاً:

```env
VITE_API_BASE_URL=https://api.aladhan.com/v1
```

---

## 📖 الاستخدام العملي

### مثال أساسي

#### استيراد واستخدام المكون الرئيسي

```tsx
import App from "./App";

function main() {
  return <App />;
}

export default main;
```

#### استخدام مكون أوقات الصلاة

```tsx
import PrayerTimes from "./prayerTimes";

function MyComponent() {
  return (
    <div>
      <h1>أوقات الصلاة في نابلس</h1>
      <PrayerTimes city="Nablus" />
    </div>
  );
}
```

### التعامل مع حالة التحميل

```tsx
import { useState, useEffect } from "react";
import PrayerTimes from "./prayerTimes";

function PrayerTimesPage() {
  const [city, setCity] = useState("Nablus");
  const [isLoading, setIsLoading] = useState(false);

  const handleCityChange = (newCity) => {
    setIsLoading(true);
    setCity(newCity);
    // يتم إعادة تعيين التحميل تلقائياً من قبل المكون
    setTimeout(() => setIsLoading(false), 1000);
  };

  return (
    <div>
      {isLoading ? (
        <p>جارِ تحميل أوقات الصلاة...</p>
      ) : (
        <PrayerTimes city={city} />
      )}
    </div>
  );
}
```

### التعامل مع البيانات المخصصة

```tsx
import { useEffect, useState } from "react";

function CustomPrayerTimes({ city, country }) {
  const [prayerTimes, setPrayerTimes] = useState(null);
  const [error, setError] = useState(null);

  useEffect(() => {
    const fetchPrayerTimes = async () => {
      try {
        const response = await fetch(
          `https://api.aladhan.com/v1/timingsByCity?city=${city}&country=${country}&method=5`,
        );
        const data = await response.json();
        setPrayerTimes(data.data.timings);
      } catch (err) {
        setError("حدث خطأ أثناء جلب أوقات الصلاة");
      }
    };

    fetchPrayerTimes();
  }, [city, country]);

  if (error) {
    return <p className="error">{error}</p>;
  }

  if (!prayerTimes) {
    return <p>جارِ التحميل...</p>;
  }

  return (
    <div className="prayer-times-grid">
      {Object.entries(prayerTimes).map(([prayer, time]) => (
        <div key={prayer} className="prayer-card">
          <span className="prayer-name">{prayer}</span>
          <span className="prayer-time">{time}</span>
        </div>
      ))}
    </div>
  );
}
```

### مثال على تخصيص طريقة الحساب

```tsx
// طريقة الحساب (Method) يمكن تغييرها حسب المنظمة:
// 1: University of Islamic Sciences, Karachi
// 2: Islamic Society of North America
// 3: Egyptian General Authority of Survey
// 4: Umm Al-Qura University, Makkah
// 5: Department of Islamic Affairs, Sharjah (الافتراضي)

const API_URL = `https://api.aladhan.com/v1/timingsByCity?city=${city}&country=${country}&method=5`;
```

---

## 🤝 المساهمة في المشروع

نرحب بمساهماتكم في تطوير هذا المشروع! اتبعوا الإرشادات التالية:

### كيف يمكنكم المساهمة

1. **استنساخ المشروع**: أنشئوا نسخة خاصة من المشروع
2. **إنشاء فرع**: أنشئوا فرعاً جديداً لميزةكم أو إصلاحكم
3. **تطوير التغييرات**:قوموا بإجراء التغييرات المطلوبة
4. **اختبار التغييرات**: تأكدوا من عمل الكود بشكل صحيح
5. **إرسال Pull Request**: أرسلوا طلب الدمج

### معايير كتابة الكود

```typescript
// ✅ ممارسات جيدة

// استخدام TypeScript للكتابة
interface PrayerTime {
  Fajr: string;
  Sunrise: string;
  Dhuhr: string;
  Asr: string;
  Maghrib: string;
  Isha: string;
}

// مكونات React باستخدام Hooks
function PrayerTimes({ city }: { city: string }) {
  const [times, setTimes] = useState<PrayerTime | null>(null);

  return <div>{times && <p>{times.Fajr}</p>}</div>;
}

// ❌ ممارسات يجب تجنبها

// استخدام any
const times: any = fetchData();

// مكونات Class بدلاً من Functional
class OldComponent extends React.Component {
  render() {
    return <div>مكون قديم</div>;
  }
}
```

### قوائم المهام المفتوحة

| المهمة                | المستوى | الوصف                        |
| --------------------- | ------- | ---------------------------- |
| إضافة مدن عربية جديدة | متوسط   | توسيع قائمة المدن المدعومة   |
| دعم التاريخ الهجري    | متوسط   | عرض التاريخ الهجري بشكل كامل |
| إضافة إشعارات الصلاة  | متقدم   | إشعارات متصفح عند وقت الصلاة |
| تطبيق PWA             | متقدم   | إمكانية العمل بدون إنترنت    |
| اختبارات Unit         | متوسط   | إضافة اختبارات Jest          |

### سياسة إدارة الإصدارات

نستخدم [Semantic Versioning](https://semver.org/):

- **major**: تغييرات غير متوافقة مع الإصدارات السابقة
- **minor**: إضافة ميزات جديدة متوافقة
- **patch**: إصلاحات أخطاء متوافقة

---

## 📄 الترخيص

### ترخيص MIT

هذا المشروع مرخص تحت **ترخيص MIT** - راجع ملف [`LICENSE`](LICENSE) للتفاصيل.

```text
حقوق النشر (c) 2024 [اسم المطور]

يُسمح بموجب ترخيص MIT لأي شخص يحصل على نسخة من هذا البرنامج
والملفات المرتبطة به، التعامل مع البرنامج دون قيود، بما في ذلك
الحق في الاستخدام والنسخ والتعديل والدمج والنشر والتوزيع
والترخيص من الباطن، والسماح للآخرين بالقيام بذلك، وفقًا للشروط التالية:

يجب تضمين إشعار حقوق النشر أعلاه في جميع النسخ أو أجزاء كبيرة منها
من البرنامج.

البرنامج موفر "كما هو"، دون أي نوع من الضمانات، سواء صريحة أو ضمنية.
```

---

## 📞 التواصل والدعم

### قنوات التواصل

| القناة                 | المعلومات                       |
| ---------------------- | ------------------------------- |
| **GitHub Issues**      | للإبلاغ عن الأخطاء وطلب الميزات |
| **GitHub Discussions** | للأسئلة العامة والمناقشات       |
| **البريد الإلكتروني**  | developer@example.com           |

### الإبلاغ عن المشاكل

عند الإبلاغ عن مشكلة، يرجى تضمين:

1. **وصف واضح** للمشكلة
2. **خطوات إعادة إنتاج** المشكلة
3. **النتيجة المتوقعة** والنتيجة الفعلية
4. **لقطة شاشة** إن أمكن
5. **معلومات النظام** (نظام التشغيل، المتصفح، الإصدار)

---

## 🙏 الاعتمادات

### أدوات ومكتبات مستخدمة

| الأداة          | الغرض                    | الرابط                         |
| --------------- | ------------------------ | ------------------------------ |
| **React**       | إطار عمل واجهة المستخدم  | https://react.dev              |
| **TypeScript**  | لغة البرمجة              | https://www.typescriptlang.org |
| **Vite**        | أدوات البناء             | https://vitejs.dev             |
| **ESLint**      | تحليل الكود الثابت       | https://eslint.org             |
| **Aladhan API** | واجهة برمجة أوقات الصلاة | https://aladhan.com            |

### الموارد المرجعية

- [وثائق React](https://react.dev/docs)
- [وثائق TypeScript](https://www.typescriptlang.org/docs)
- [وثائق Vite](https://vitejs.dev/guide)
- [وثائق Aladhan API](https://aladhan.com/timings-api)

---

<div align="center">

**⭐ إذا أعجبك المشروع، لا تنسَ إعطاء النجمة! ⭐**

**صُنع بـ ❤️ للمسلمين حول العالم**

</div>
